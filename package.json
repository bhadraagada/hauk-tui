{
  "name": "hauktui-monorepo",
  "private": true,
  "version": "0.0.1",
  "description": "haukTUI monorepo: tokens, primitives, components, and CLI for terminal UIs (Ink + TypeScript)",
  "scripts": {
    "build": "pnpm -r run build",
    "clean": "pnpm -r run clean || true",
    "demo": "pnpm build && pnpm --filter examples demo"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "react": "^18.0.0",
    "ink": "^4.4.1"
  }
}
