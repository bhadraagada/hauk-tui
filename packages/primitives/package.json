{
  "name": "@hauktui/primitives",
  "version": "0.0.1",
  "type": "module",
  "description": "Low-level TUI primitives for Ink (FocusRing, KeymapProvider)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "if (Test-Path dist) { Remove-Item -Recurse -Force dist }"
  },
  "peerDependencies": {
    "ink": "^5.2.1",
    "react": "^18.3.1"
  },
  "dependencies": {
    "@hauktui/tokens": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^18.0.0",
    "typescript": "^5.6.0",
    "ink": "^5.2.1",
    "react": "^18.3.1"
  },
  "license": "MIT"
}
